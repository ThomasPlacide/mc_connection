apiVersion: v1
kind: Service
metadata:
  name: mcco_app_service
  labels:
    app: mcco_app
spec:
  type: ClusterIP
  selector:
    app: mcco_app
  ports:
  - protocol: TCP
    port: 5001
    targetPort: 5001